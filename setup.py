from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a small ml project for emotion detection with aws s3 as remote dvc storage',
    author='prashant',
    license='',
)
